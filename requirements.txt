# Phase 1: key derivation and encryption.
# Phase 3: FastAPI + ASGI server; pydantic comes with FastAPI.
# Pinned for reproducible installs (see project rules: dependency pinning).
cryptography>=42.0.0,<47
fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1
# Testing (backend-tester runs: pytest tests/). httpx required by FastAPI TestClient.
pytest>=8.0,<9
httpx>=0.27,<1
# Optional: install for error tracking when VAULT_SENTRY_DSN is set.
sentry-sdk[fastapi]>=2.0,<3
